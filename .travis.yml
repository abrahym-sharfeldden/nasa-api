dist: xenial          #required for Python >= 3.7
language: python
python: '3.7'
sudo: required


#command to run tests
script:
  - pytest

after_Script: cd ~

deploy:
  provider: pages
  skip_cleanup: true
  github_token: 
  keep_history: true
  app: nasa-api
  on:
    branch: master